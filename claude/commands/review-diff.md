---
description: Review current diff for bugs, regressions, and missing verification.
argument-hint: "[optional focus]"
---

Review the current diff with this focus:

`$ARGUMENTS`

Use `~/.claude/bin/mct classify` to classify changed files before reviewing.

Prioritize correctness, regressions, security, accessibility, typing, performance, and missing tests. Findings first, summary second.
