---
description: Show MCT project state, TODO queue, changed surfaces, recommended checks, and recent receipts.
argument-hint: "[--md]"
---

Run:

```bash
~/.claude/bin/mct status --md
```

Use the output to summarize current TODO state, next runnable task, blocked tasks, changed surfaces, checks, and recent receipts.
