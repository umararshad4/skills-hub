---
description: Prepare current work for PR with checks and concise PR text.
argument-hint: "[optional base branch or focus]"
---

Prepare this work for PR:

`$ARGUMENTS`

Use `~/.claude/bin/mct status` first to classify changed files, TODO status, recommended checks, and recent receipts.

Inspect the diff, run appropriate checks, run React Doctor if React files changed, self-review, and draft a concise PR title/body without tool branding.
