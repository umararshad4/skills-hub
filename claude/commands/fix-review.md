---
description: Fix actionable review findings with scoped edits and targeted checks.
argument-hint: "<review findings>"
---

Fix these review findings:

`$ARGUMENTS`

Only address actionable findings. Do not add unrelated cleanup. Run targeted checks for the changed surfaces and summarize what was fixed.
